# 10 分钟演示视频录制计划书（XH-202630 · 对照榜题完美版）

> 用途：与前端队友录制题目硬性要求的「10 分钟以内系统演示视频」。
> 榜题原文对视频的要求：**清晰展示——从差异化学情画像输入 → 多智能体协同调度与交互过程可视化 → 最终个性化领域知识资源生成的完整闭环**。
> 本计划书把**每条答题要求 / 评分项都映射到具体时间段**，确保评委在视频里能逐条找到证据。
> 3 个问题严格对应我们已最终化、零假标的 showcase 用例 `TC-001 / TC-020 / TC-048`，视频内容与提交的 `data/io_examples/`、`benchmark_evidence_100.json` 完全一致。

---

## 〇、评分点 → 视频段落 可追溯矩阵（录制/自检总纲）

| 榜题条款 | 要求要点 | 视频必须出现的画面 | 计划时段 | 对应评分 |
|---|---|---|---|---|
| 答题(1) 场景覆盖 | 人工智能/软件开发等垂直领域；体现不同背景学习者适配 | 开场点明领域；差异化适配对比段（TC-001完整 + TC-020/048 快速切换） | 0:00 / 0:35–2:20 | 完整30 / 实用30 |
| 答题(2) 多 Agent 闭环 | ≥3 个职责分工 Agent；"分析-生成-校验-决策"闭环 | 前端 7 步 WS 流转动画（诊断→生成→审核→整合） | 0:40 起每段 | 完整30 / 体验15 |
| 答题(2) 知识库+画像 | 整合先验画像 + 领域知识库 | 提到 numpy_kb 切片 + 画像输入表单 | 0:40 / 7:40 | 完整30 / 实用30 |
| 答题(2) 3 形态资源 | 定制化资源 / 实操指南 / 分阶测验 | 同一问题产出讲义+代码+测验三形态 | 1:30 / 4:00 | 完整30 / 实用30 |
| 答题(3)① 可视化报告 | 盲区定位 + 难度曲线 + 学习路径 | `/api/report` 三张图同屏展示 | **2:10–3:10** | 完整30 / 体验15 / 实用30 |
| 答题(3)② 动态迭代 | 按答题正确率降维/进阶 | 测验后故意答错<60%触发降维重生成 / ≥85%触发进阶 | **3:20–5:30** | 完整30 |
| 答题(4)① 辩论消幻觉+溯源 | 交叉验证消除幻觉；高保真溯源 | 裁判团三维度结论 + `verify_statement` 溯源标记 | 5:10–6:30 | 创新25 / 实用30 |
| 答题(4)② 动态追问导学 | 启发式交互导学 | 系统主动追问 / 衍生问题 | 5:10–6:30 | 创新25 / 体验15 |
| 答题(5) 数据合规 | 脱敏 / 合规使用 | 本地部署 + 数据隔离 + 无 PII 说明 | 7:40–8:50 | 完整 / 实用 |
| 评选·实用30 指标 | 幻觉<5% / 适配≥85% / 覆盖≥90% | 诚实指标回顾（87.9% 不造假） | 7:40–8:50 | 实用30 |
| 评选·体验15 | 调度可视化流畅、资源排版清晰 | 7 步流动画 + 三形态排版 | 全程 | 体验15 |

---

## 一、录制前依赖核对（⚠️ 录制前必须向前端队友确认，否则优先补开发再录）

上一次录的片子缺了"可视化报告"，多半是前端没渲染 `/api/report` 或没点进去。**以下任一项缺失都会直接丢分，必须先确认：**

- [ ] **前端有 `/api/report/{session_id}` 可视化页**，且能渲染 **盲区热力图 + 资源难度匹配曲线 + 学习路径规划图** 三张图（答题3① 硬性可视化项，缺一不可）
- [ ] 前端有 **WS `/ws/{task_id}` 驱动的 7 步调度流动画**（PROFILING→DISPATCHING→GENERATING→REVIEWING→FOCUSING→JUDGING→FORMATTING）
- [ ] 前端能同屏或顺次展示 **3 形态资源**（讲义 / 实操指南 / 分阶测验）
- [ ] 前端有 **测验提交** 并能触发 **降维解释 / 进阶挑战**（答题3②）
- [ ] 前端展示 **裁判团结论**（事实/逻辑/远见 + 置信度）与 **溯源标记 / 引用状态**
- [ ] 前端有 **自然语言画像输入入口**（或至少结构化画像表单）——建议录一段自然语言诊断，强化 ProfileAgent 卖点

---

## 二、怎么录制（技术准备 · 前后端两台机器，勿默认同屏）

### 2.1 环境与连通（录制前必须跑通）
1. **启动后端**（PyCharm 用已生成的 `Backend Server` run config，reload=False，避免僵尸占 8000）：
   ```
   .venv\Scripts\python.exe -m backend.main
   ```
2. **开 Cloudflare 隧道**（队友连这个 URL，不在局域网也能访问）：
   - 程序：`cloudflared.exe/cloudflared-windows-amd64.exe`
   - 启动后抓 `https://*.trycloudflare.com` 域名（**每次重启换域名，旧 URL 立即失效**）
   - 前端把 API base 指向该 URL，先 `curl <URL>/health` 验 200 再录
3. **前端**：队友确认能发 `POST /api/ask` 并接收 `WS /ws/{task_id}` 实时状态。

### 2.2 录屏设置（OBS Studio）

- 工具：OBS Studio（免费），**1080p / 30fps**，采集「系统音频 + 麦克风旁白」。

#### 「多智能体协同调度可视化」证据从哪来
| 来源 | 所在机器 | 形态 | 必要性 |
|---|---|---|---|
| **前端 WS 驱动的 7 步流转动画** | **队友机器** | 打磨过的产品级可视化 | ✅ **必须，队友屏幕已自带** |
| 后端终端 loguru 原始调度日志 | **本机** | 一行行 `FSM转移`/`段N审核`/`降维解释完成` 原始 trace | ❌ 可选补充 |

> 评分要求的是"调度过程可视化"，**前端那套 7 步动画已满足**，队友无需看到你的后端终端。右 40% 后端终端屏不是跨机器硬要求，只是可选技术佐证。

#### 两种落地方式（二选一）
- **方式①（推荐）：整套在你本机录**。浏览器打开队友部署的前端（走 cloudflared 隧道 URL）放左 60%，你的后端终端放右 40%，OBS 录本机桌面 → 正好 60/40 分屏、零同步问题。队友仅需保证前端在线 + 隧道通畅。
- **方式②：各录各的再拼接**。队友录前端、你录后端终端，剪映/PR 拼接（**拼接完全可行**）。代价：多一道剪辑、有音画对齐风险。

- **旁白**：边操作边讲「现在输入的是哪种学情」「现在哪个 Agent 在工作」「生成了哪几种资源」。

### 2.3 预演与控时
- **正式录前空跑 3 遍**：3 个问题各跑一次，确认都在 ~2 分钟内出结果（避免 API 超时卡顿）。
- 总时长严格 **≤ 10:00**，建议 **9:30** 收尾留余量。OBS 开「录制计时」。
- 隧道若中途断：立刻重启 cloudflared 拿新 URL → 前端重连 → 从断点场景续录（不要硬扛黑屏）。

---

### 录制策略原则（回应：优点这么多，是否每个都要展现？）

**不必每个优点都摊。** 10 分钟是硬约束，塞太多小构思 → 每个都浅、评委记不住，反而稀释核心卖点。正确做法是**聚焦评分密度最高的优点**（直接对应评分条款），其余在《作品介绍》/方案书里写，不进视频。

**三组成像是否必须？分两层：**
- **视频里"学情画像输入"这一环必须出现**——赛题「作品提交形式」明确要求视频起点是"差异化学情画像输入"，不能省。
- **"≥3 组学情用例"是提交材料硬指标**（答题6 实用价值 / 测试数据），已由 `data/io_examples/` 的 TC-001/020/048 满足，**不靠视频罗列**。视频只需让评委"看到系统确实适配不同背景"即可。

**因此本片采用"压缩三组、把时间给优点"策略：**
- 三组成像压缩为 **一个差异化适配对比段**（约 1:45）：TC-001 完整跑（基准证据，原句原画像），TC-020/048 仅快速切换对比"背景不同→产出不同"，不各展开三形态。
- 省下的时间全部给 **评分密度最高的优点**：动态决策闭环（低准确率自动降维重生成）、辩论消幻觉+溯源、贡献记忆闭环。

## 三、录制什么（分镜头脚本 · 10 分钟时间轴）

| 时间 | 镜头 / 操作 | 旁白要点（对齐榜题条款） |
|---|---|---|
| **0:00–0:35** | 开场：系统定位页 + 架构一句话 + 闭环总览 | 本系统面向**人工智能/软件开发**领域个性化技能培训；11 个 Agent 模拟真实业务流，弃用"单体大模型包打天下"；本视频展示：差异化学情画像→多 Agent 协同调度可视化→个性化资源生成的**完整闭环**（答题1 / 选题意义） |
| **0:35–2:20** | **学情画像输入 + 差异化适配（压缩为一段）**：①点「TC-001 快速画像」→自动选中 cs_student（CS学生/入门/LLM基础/深入理解原理）→输入问题①→生成，展示三形态（讲义+示例+5测验）；②快速切 TC-020（developer/中级/Agent框架）、TC-048（developer/进阶/Agent框架）按钮，**同屏对比**三者生成的讲义深度/风格明显不同 | 展示学情诊断 Agent 锚定盲区 + 领域生成 Agent 依托 KB 生成 + 审核裁判交叉验证（分析-生成-校验-决策）；重点口播：**同一领域对不同背景产出不同难度与风格**，坐实"差异化适配"（答题1 / 完整30 / 实用30）。TC-001 为基准证据必须原句原画像；TC-020/048 只做快速切换对比，不各展开三形态（已在 TC-001 展示过） |
| **2:20–3:20** | **可视化报告**：`/api/report/{session_id}` → 盲区热力图 + 难度曲线 + 学习路径图 | 答题3① 硬性可视化项，三图缺一不可（完整30 / 体验15 / 实用30） |
| **3:20–5:30** | **★动态决策高潮（重点加长）**：①做 TC-001 测验 → **故意答错使正确率<60%** → 系统经 WS 推 `REDIMENSION` 状态 → 自动**降维重生成整份更浅资源包**（讲义+实操+测验）；②另演示一次正确率≥85% → `ADVANCE` 追加 1 道进阶挑战题 | 答题3② 动态迭代的最硬证据：系统按答题正确率多智能体协同决策"降维/进阶"，**不是静态单向资源**。同时坐实完整30分闭环最后一环"动态决策更新"（完整30 / 实用30 / 创新25） |
| **5:30–7:00** | **创新性亮点**：裁判团交叉验证/辩论消幻觉 + `verify_statement` 知识溯源 + 动态追问导学 | 多 Agent"辩论与交叉验证"消除幻觉、高保真溯源与约束生成；点明**幻觉率 3.0%**；系统主动启发式追问打破静态输入（答题4 / 创新25 / 实用30 / 体验15） |
| **7:00–8:00** | **贡献记忆闭环（动态决策更新）**：「多智能体协同优化反馈」卡片 → α=0.3、Agent 贡献分排行、最近贡献流、淘汰记录 | 完整闭环最后一步：每次任务后记录贡献、EMA 更新表现分、动态调 α、淘汰低表现 Agent——"越用越聪明、优胜劣汰"（完整30 / 创新25）。数据来自 GET /api/memory_stats（录制前重启后端） |
| **8:00–9:00** | 合规 + 指标回顾 | 本地部署/数据隔离/无 PII 脱敏（答题5）；指标：幻觉 3.0%/适配 100%/谬误 0%/**覆盖率 87.9%（诚实未达 90%）**；KB 切片 30532 chunks（实用30） |
| **9:00–10:00** | 收尾：闭环回顾 | 学情→调度→资源→反馈→**贡献记忆迭代**，完整闭环回放（完整30） |

---

## 四、要录制的问题（3 组差异化学情，严格对应 TC-001/020/048）

> 📌 **录制策略（压缩三组、时间给优点）**：TC-001 完整实时跑通（含画像输入→三形态→报告→测验）；TC-020/048 **不各展开三形态**，仅在 0:35–2:20 段快速切换画像按钮 + 同屏对比各自已生成资源的深度/风格差异，坐实"差异化适配"即可。三者的完整 I/O 证据由 `data/io_examples/` 提交文档承载，不靠视频逐组罗列。

> ⚠️ **硬性红线（录前必读）**：本节的**画像取自 `data/io_examples/bm_TC-XXX_io.json` 的 `input_profile` 真值**，**问题文本取自 `tests/test_cases_100.json` 的基准原文**。二者必须与提交的 I/O 示例文档**逐字一致**——**禁止改写问题措辞、禁止替换画像字段**。理由：评委可拿视频对照我们提交的 `data/io_examples/` 与 `benchmark_evidence_100.json`；若不一致会暴露"视频与证据不是同一组"。三组刻意覆盖「入门 / 中级 / 进阶」三档 + 「CS 学生 / 开发者」不同背景，直接证明「差异化适配」（答题1）。

### 问题① — 入门档（对应 TC-001）
- **学情画像（io_examples 真值，已对齐基准）**：`knowledge_level=入门` / `background=cs_student` / `domain_hint=["LLM基础"]` / `question_type=概念理解` / `current_goal=深入理解原理` / `complexity_estimate=单领域` / `intent_type=generation`
- ℹ️ `current_goal=深入理解原理` 是系统对基准 `learn_basics` 的**正式中文标签**，与前端 `current_goal` 枚举「快速上手应用 / 深入理解原理 / 项目落地 / 算法研究」完全一致。视频、io_examples、导出脚本三处必须统一用 `深入理解原理`，**禁止改成"学习基础"**——后者不在枚举内，后端会忽略该字段并退回自动诊断，导致演示画像不确定。
- **输入问题（test_cases_100.json 原文，禁止改写）**：「在大语言模型中，什么是 Token？它与"词"有什么区别？」
- **预期产出**：基础概念讲义 + 可运行小示例 + 5 道概念测验（含选择/判断/简答）

### 问题② — 中级档（对应 TC-020）
- **学情画像（io_examples 真值）**：`knowledge_level=中级` / `background=developer` / `domain_hint=["Agent框架"]` / `question_type=概念理解` / `current_goal=项目落地` / `complexity_estimate=单领域` / `intent_type=generation`
- **输入问题（test_cases_100.json 原文，禁止改写）**：「请解释 ReAct（Reasoning + Acting）范式的工作流程，Thought-Action-Observation 循环是如何进行的？」
- **预期产出**：含 T-A-O 循环的讲义 + 可执行代码示例（实操指南）+ 5 道进阶测验

### 问题③ — 进阶档（对应 TC-048）
- **学情画像（io_examples 真值）**：`knowledge_level=进阶` / `background=developer` / `domain_hint=["Agent框架"]` / `question_type=操作步骤` / `current_goal=项目落地` / `complexity_estimate=单领域` / `intent_type=generation`
- **输入问题（test_cases_100.json 原文，禁止改写）**：「请设计一个多 Agent 协作流程，包含规划 Agent、执行 Agent、审查 Agent，说明数据如何流转。」
- **预期产出**：多 Agent 规划类讲义 + 分步操作指南 + 5 道设计型测验；verdict 可能为 `low_confidence_passed`（正常低置信通过，非兜底）

> 📌 **前端表单录入说明（更正）**：TC-001 快速画像按钮点击后**前端直接选中 `background=cs_student`（CS 学生）**，无需任何枚举映射。界面上**只显示 CS 学生**，绝对不要出现"有 Python 基础"字样（那既不是基准画像也不是问题要求，是之前计划书的笔误）。`cs_student` 由后端 `_coerce_profile` 容错识别为合法背景，可正常跑通。

---

## 五、必展示亮点清单（对照得分点，逐项打勾）

- [ ] **多智能体调度可视化**：`WS /ws/{task_id}` 实时显示学情诊断→领域生成→审核裁判的状态流转（前端 7 步动画）
- [ ] **"分析-生成-校验-决策"闭环**：前端流动画体现四个阶段，非单体直出
- [ ] **知识库整合**：提及 numpy_kb 切片（30532 chunks）接地生成
- [ ] **3 形态资源**：讲义（lecture）、实操指南（practice_guide）、分阶测验（quiz）同屏或顺次展示
- [ ] **可视化报告三要素**：盲区热力图 + 难度曲线 + 学习路径（来自 `/api/report`，**缺一不可**）
- [ ] **动态迭代**：降维解释 / 进阶挑战 按答题正确率自动追加（来自 `/quiz_submit` 触发）
- [ ] **辩论消幻觉 + 溯源**：裁判团三维度结论 + `verify_statement` 溯源标记 / 引用状态（已验证 / 未命中KB检索）
- [ ] **动态追问导学**：系统主动发起启发式追问（来自 `/api/ask` followup）
- [ ] **差异化适配**：问题①与②用不同画像，资源难度/风格明显不同（CS 生 vs 开发者）
- [ ] **数据合规**：本地部署 / 数据隔离 / 脱敏说明

---

## 六、避坑与质检清单（录完必查）

1. **覆盖率诚实表述**：只说「87.9%」，绝不可说「90% 以上」或「全覆盖」——评审若核对证据会发现矛盾，违反竞赛铁律（实用30 已知扣 5–10 分，接受诚实扣分）。
2. **三个问题必须严格使用 TC-001/020/048 原文**，画像逐字段对齐 io_examples，禁改写。
3. **可视化报告段必须含三张图**（盲区热力图/难度曲线/学习路径），缺一张即不满足答题3①。
4. **测验结果正确率 ≥60%**：上一版出现 0% 观感极差，补录时务必答对至少 3 题或展示"先错后改进"闭环。
5. **修复参考文献重复堆砌**：同一文档重复列出十几次像溯源 bug，录制前让前端/后端去重。
6. **不要展示报错/强制放行流程**；TC-048 的 `low_confidence_passed` 是正常低置信通过，旁白说明即可。
7. **隧道断了立刻重启换 URL**，前端重连后从断点续录；不留黑屏/卡顿。
8. 三个问题必须都跑通且闭环完整，无「生成到一半没了」的片段。
9. 录完用剪映粗剪：删停顿、保证 ≤10:00、导出 1080p MP4。
10. 视频文件名建议：`作品演示视频_领域知识个性化生成与多智能体协同决策系统.mp4`

---

## 附：前端需对接的端点速查

| 端点 | 用途 | 视频中出现场景 |
|---|---|---|
| `POST /api/ask` | 提交问题+学情画像，返回 `task_id` | 三个场景的输入 |
| `WS /ws/{task_id}` | 实时 Agent 调度状态流 | 调度可视化（前端 7 步动画） |
| `GET /api/status/{task_id}` | 任务状态轮询 | 备选状态展示 |
| `GET /api/report/{session_id}` | 盲区/难度曲线/路径报告 | **可视化报告段（2:10–3:10）** |
| `POST /quiz_submit` | 提交测验结果，触发降维/进阶 | 动态迭代段 |
| `POST /api/ask`（followup） | 动态追问/启发式导学 | 创新亮点段 |
