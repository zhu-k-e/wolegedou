# 演示视频完整拍摄计划（终版 v2 · 怎么录 / 录什么 / 旁白说什么）

> 适用赛题：XH-202630
> 录制载体：队友真实前端 + 真实后端（已证实走真后端、轮询拿状态，无演示数据问题）
> 硬性约束：**视频 ≤ 10 分钟**，必须展示完整闭环「差异化学情 → 多 Agent 调度可视化 → 资源生成 → 测验自适应 → 报告」。
> 最后更新：2026-08-28（v2：新增镜头 5.5「差异化适配实证」，TC-020 / TC-048 均给真实画面，不再只露标签）

---

## 〇、录制前必做清单（开录前一次性搞定）

### A. 后端状态（已全部就位，无需再改）


| 项               | 状态 | 说明                                                                                                 |
| ---------------- | ---- | ---------------------------------------------------------------------------------------------------- |
| 测验阈值 0.85    | ✅   | 唯一门槛（`orchestrator.py:1254`），前端消费 `action` 即可                                           |
| 裁判三维度分数   | ✅   | `review_summary` 已加入 `AskResponse`，前端可取 fact_accuracy / logic_completeness / pedagogical_fit |
| 知识引用去重     | ✅   | 后端构建`knowledge_refs_display` 时已按 `(source, content_summary)` 去重                             |
| 报告历史画像合并 | ✅   | 同一 session 多轮提问后热力图会累积（`report.py` 修复）                                              |
| 难度匹配曲线数据 | ✅   | `/api/report` 已返回 `difficulty_match`，前端待渲染                                                  |

> ⚠️ 录制期间**不要改任何后端文件**，uvicorn 热重载会短暂断连。

### B. 前端待渲染项（队友正在改，开录前逐项核对）

1. **报告第 4 个 tab「资源难度匹配曲线」**（赛题硬性三图+此曲线，目前缺）——数据后端已给，前端加 tab 画双线图。
2. **裁判三维度分数条**（之前只显示一行字）——后端已返回 `review_summary`，前端画三条分数条 + 定性结论。
3. **启发式追问 UI**——测验提交后渲染 `r.followup_questions` 为「💡 系统想进一步问你：」区块（判空）。
4. **引用文件名截断**——过长源名做 `…` 省略 + hover 看全名，别撑破面板。
5. **测验阈值标签**——显示 0.85（别写 60%/80%）。
6. **隐藏调试 JSON 横幅**——录制时不展示后端原始响应 JSON。
7. **实操指南清晰化**——三形态之一，资源 tab 里要能看清、可操作。
8. **调度流水线 8 步已确认**：真实前端渲染为「学情诊断→Agent调度→内容生成→质量审核→结果聚合→裁判评估→资源整理→任务完成」8 张卡片，对应代码状态 `PROFILING→DISPATCHING→GENERATING→REVIEWING→FOCUSING→JUDGING→FORMATTING→COMPLETE`。测验/降维/进阶/追问不显示在主流水线，而是在镜头 5 单独展示为自适应闭环。

### C. Session 与数据策略（关键）

- **⚠️ 演示输入锁定三组已定稿学情 I/O 示例（TC-001 / TC-020 / TC-048）**，问题原文与画像一律从 `data/io_examples/bm_TC-0XX_io.json` 取，**不得临时编造问题**——评审若发现视频问题不在交付样例里，会被质疑真实性。
  - TC-001：入门 / 计算机专业 / 深入理解原理 / 意向 LLM基础 → 问「在大语言模型中，什么是 Token？它与"词"有什么区别？」
  - TC-020：中级 / 开发者 / 项目落地 / 意向 Agent框架 → 问「请解释 ReAct（Reasoning+Acting）范式的工作流程，Thought-Action-Observation 循环是如何进行的？」
  - TC-048：进阶 / 开发者 / 项目落地 / 意向 Agent框架 → 问「请设计一个多 Agent 协作流程，包含规划 Agent、执行 Agent、审查 Agent，说明数据如何流转。」
- **新建专用录制 session**，固定 `session_id`（如 `rec_tc001_demo`），全程不刷新、不换——否则报告数据零散、不连续。
- **录前预热（必做）**：用该 session 真实跑 **两轮**——
  - 一轮故意答错使正确率 < 85% → 触发**降维（生成更简单题目）**；
  - 一轮全对使正确率 ≥ 85% → 触发**进阶挑战**。
  - 目的：① KB 预热（首问最慢）；② 报告四图有真实数据；③ 难度曲线出现 `matched/too_easy/too_hard` 多状态好看。
- **时间隐患**：单次问答 ≈ 84 秒（真实跑批实测）。处理：预热那两轮不进正片；正片里的现场问答，**等待段剪掉**，只保留「提交 → 切结果」的瞬间 + 旁白解说。

### D. 指标数字（严守诚实铁律）

- 旁白里所有百分比**必须从最新 benchmark 实测报告取值**，禁止凭记忆填。
- 已知框架：**适配率、幻觉率、谬误率达标；覆盖率一项未达 90% 目标线——如实说明，绝不造假**（竞赛铁律：假比低分更致命）。
- 录制前让队友把报告里的真实数字念给你，填进下方旁白占位符。

---

## 一、分镜总览（≤10 分钟时间轴）


| 时段        | 镜头                  | 展示后端真能力                                              | 对应评分  |
| ----------- | --------------------- | ----------------------------------------------------------- | --------- |
| 0:00–0:40  | 开场 + 项目定位       | ——                                                        | 完整/创新 |
| 0:40–1:50  | 学情画像输入与生成    | 差异化画像 / 11 Agent 体系                                  | 完整 30   |
| 1:50–3:40  | 多 Agent 调度可视化   | 8 步主链路状态机（PROFILING→…→COMPLETE）+ 每步负责 Agent | 创新 25   |
| 3:40–5:20  | 资源生成 + 裁判三维度 | 三形态 + 量化裁判 + 溯源去重                                | 实用 30   |
| 5:20–7:00  | 测验与自适应闭环      | 0.85 门槛 / 降维 / 进阶 / 追问                              | 完整 30   |
| 7:00–8:30  | ⭐ 差异化适配实证      | **TC-020 中级 + TC-048 进阶 双画像实跑对比**（赛题差异化硬要求） | 实用 30   |
| 8:30–9:10  | 报告页四图            | 画像/热力图/路径/**难度曲线**（压缩）                        | 实用 30   |
| 9:10–9:35  | 贡献记忆 / 能力全景   | 11 Agent 排行 + α + 跨域（压缩）                            | 创新 25   |
| 9:35–9:55  | 闭环总结（压缩）      | 回放完整链路                                                | 完整 30   |
| 9:55–10:00 | 创新点收尾（压缩）    | 辩论消幻觉 / 启发式追问 / 量化裁判                          | 创新 25   |

---

## 二、逐镜头脚本（画面 + 操作 + 旁白）


### 镜头 1 · 开场（0:00–0:40）

**画面**：浏览器打开前端首页。页面顶部显示 **「AI智能学习助手 · 多智能体协同学习系统 · Multi-Agent Learning System」** 标题，右上角有绿色 **「系统运行正常」** 状态徽标（表示后端在线）。主体是 **「创建学习任务」** 卡片：学习问题输入框、学习目标 chips（快速上手应用 / 深入理解原理 / 算法研究 / 项目落地）、学习资源需求复选框（学习讲义 / 实操指南 / 测试题）、可折叠的「学情画像（选填）」面板。卡片底部有一行小字：**系统流程：学情诊断 → Agent调度 → 内容生成 → 审核裁判 → 资源输出**。页面再往下是「学习历史记录」区域。

**操作（在队友电脑上这样执行）**：

1. 打开浏览器进入首页，**等待 1 秒让状态徽标稳定显示绿色**。
2. 鼠标从左上角标题缓慢滑到右上角 **「系统运行正常」** 徽标，停留 2 秒（不用点击，只展示）。
3. 鼠标下滑到 **「创建学习任务」** 卡片，依次轻轻掠过：学习问题框 → 学习目标 chips → 学习资源需求复选框 → 底部系统流程文字，全程约 6 秒。
4. 最后鼠标停在页面中下部「学习历史记录」区域上方 1 秒，暗示系统已有真实运行痕迹，然后切到下一个镜头。

**旁白**：

> "大家好，这是我们团队的作品——**AI 智能学习助手**，一个面向人工智能与软件开发领域的**领域知识个性化生成与多智能体协同决策系统**（本次挑战杯 XH-202630 赛题作品）。
> 注意右上角这个 **『系统运行正常』** 的标识：它说明后端真实在线。等会儿你看到的所有结果，都不是预置假数据，而是后端一个个 Agent 真实跑出来的。
> 它的核心不是『一个大模型直接答题』，而是**先通过学情诊断 Agent 读懂你现在的水平，再调度一整套专业智能体团队，为你生成量身定制的学习资源，并持续追踪你的掌握情况**。页面底部这行流程，就是它会走的工作链路。
> 接下来我用一条真实的学习轨迹，带你看它怎么工作。"
>

### 镜头 2 · 差异化学情画像（0:40–1:50）

**画面**：在「创建学习任务」卡片里，点击 **「学情画像（选填）」** 折叠面板展开。面板内显示：① **赛题学情用例 ≥3 组** 的可点击标签（TC-001 入门·CS学生 / TC-020 中级·开发者 / TC-048 进阶·开发者）；② 结构化字段：**知识水平**（入门/中级/进阶）、**学科背景**（文科/理科_无编程/有Python基础/有ML基础/CS学生/开发者）、**学习目标**（快速上手应用/深入理解原理/算法研究/项目落地）、**问题类型**、**复杂度估计**、**意图类型**、**领域关键词**、**理论测试成绩**。上方可回到「学习问题」框——用自然语言描述需求。

**操作（在队友电脑上这样执行）**：
1. 鼠标点开「学情画像（选填）」折叠面板，停留 1 秒，让镜头看清字段全貌。
2. **先依次掠过三个用例标签**（每标签停约 0.5 秒，不点击）：「TC-001 入门·CS学生」→「TC-020 中级·开发者」→「TC-048 进阶·开发者」，证明系统内置**多组差异化学情**（入门/中级/进阶全覆盖），然后鼠标回到 **TC-001**。
3. 点击 **「TC-001 入门·CS学生」**——系统自动把下方字段填成 TC-001 定稿值（知识水平=入门、学科背景=CS学生、学习目标=深入理解原理、领域关键词=LLM基础）。**不临时手编，直接复用已定稿用例。**（TC-020/048 的完整实跑放在镜头 5.5「差异化适配实证」，本镜头先把三组标签都亮出来，埋下"差异化"伏笔。）
4. 鼠标上移到「学习问题」框，轻点一下，框内已是 TC-001 真实问题「在大语言模型中，什么是 Token？它与"词"有什么区别？」——镜头停 2 秒，证明问题与画像同属 LLM 基础领域。
5. 不在此镜头点「生成讲义」，**留到镜头 3 提交**，避免提前出结果。

**旁白**：

> "赛题的第一要求，是从**差异化的学情**开始。同一个知识点，初学者和进阶者的讲法应该完全不同。
> 你看这块——系统提供了**结构化的学情画像面板**：知识水平、学科背景、学习目标、领域关键词都可以精确指定；同时，上面这个输入框又允许你**直接用自然语言描述需求**。两种入口，一个道理：先把自己说清楚。
> 系统内置了多组差异化学情用例，从**入门**（TC-001）到**中级**（TC-020）再到**进阶**（TC-048），覆盖不同背景和领域。我直接选用团队已定稿的**学情用例 TC-001**——入门水平、CS 学生背景、想深入理解原理、聚焦 LLM 基础领域。提交后，学情诊断智能体就会据此生成画像，作为后面所有『量身定制』的依据。这正是『差异化辅导』的出发点。"

### 镜头 3 · 多 Agent 调度可视化（1:50–3:40）

**画面**：板块②「多智能体调度可视化」——**8 张卡片式状态机**，横向一字排开，每张卡片含：中文阶段名 + 英文状态（如 `PROFILING`）+ 负责 Agent 标识（`.ag` 字段）+ 进度色（灰=待执行 / 蓝=进行中高亮脉冲 / 绿=已完成）。8 张卡片顺序为：**学情诊断 PROFILING → Agent调度 DISPATCHING → 内容生成 GENERATING → 质量审核 REVIEWING → 结果聚合 FOCUSING → 裁判评估 JUDGING → 资源整理 FORMATTING → 任务完成 COMPLETE**。卡片实时随后端推进依次点亮、变绿。

**操作（在队友电脑上这样执行）**：
1. 回到「创建学习任务」卡片，确认学习问题框已是 TC-001 真实问题、学情画像已载入 TC-001 定稿值。
2. 鼠标点击 **「生成讲义」** 按钮（真实前端触发调度的入口），**停留 1 秒**让「学情诊断」卡片先变蓝。
3. 镜头**紧贴 8 张卡片**，每当前步卡片变蓝（进行中）暂停约 3 秒，旁白念该步负责 Agent 与职责；该步完成后卡片变绿，镜头随之下一张。
4. 全程不切走，直到「任务完成 COMPLETE」变绿，再进入镜头 4。

**旁白**：

> "你的问题是：**在大语言模型中，什么是 Token？它与"词"有什么区别？**（TC-001，LLM 基础领域）。我点下『生成讲义』，系统就开始工作了。注意看这条流水线——它不是一步到位，而是被拆成 8 个阶段，每张卡片对应一个智能体在干活。
> 先是**学情诊断**确定你的水平；再**Agent 调度**选出最懂 LLM 基础的领域智能体；**内容生成**初稿；**质量审核**做事实与逻辑审查；**结果聚合**针对你的薄弱点补讲；**裁判评估**三维度打分；**资源整理**后**任务完成**。
> 整套系统背后是 **11 个各有所长的专业智能体**，每一步都由最合适的那一个来负责——这就是『多智能体协同』，而不是单个模型硬扛。"

### 镜头 4 · 资源生成 + 裁判三维度（3:40–5:20）

**画面**：生成完成后进入「AI 生成结果」页(资源展示区)。页面分上下两块:

- **上半 · AI 生成结果**(Markdown 讲义正文,含 6 步大节 + 「RAG 检索结果」标注 + 「引用片段」列表,每条带 `LangChain RAG Tutorial.md` 等源文件名 + 「LLM 推理过程」步骤)。

- **下半 · 多 Agent 裁决结果**(紫蓝渐变面板):
  - 顶部**综合评价**区块:三档裁判(事实审查 // 逻辑审查 // 适用性审查)每段通过情况 + 置信度(85% / 90% / 60% 等);
  - 三条**质疑评分**进度条:事实准确性 100% / 论证完整性 80% / 参考来源 90%(绿/橙/绿);
  - 下方**参考来源**列表:每条 `LangChain RAG Tutorial.md; LangChain RAG Tutorial.md; ...`(自动去重后的引用片段)。

> 截图实拍(2026-08-24,已核对):资源三形态中讲义为 Markdown 长文,裁判结果以紫蓝渐变面板呈现,引用去重后按 source 分组罗列。

**操作(在队友电脑上这样执行)**:
1. 生成完成后，切到 **「讲义」** 视图，镜头特写讲解正文 + 引用列表，逐条点过 `已验证` 徽标（绿色）停留 2 秒，证明溯源核验真实存在（非装饰）。
2. 再切到 **「裁判」/ 裁判结果面板**，展示三条分数条（事实准确性 / 逻辑完整性 / 教学适用性）与定性结论，停留 3 秒。
3. 如前端有「实操指南」「测试题」独立视图，顺手切一下证明三形态齐全（不展开，留到镜头 5 讲测验）。

**旁白**：

> "针对你的问题，系统一次性生成**三种形态**的资源：系统讲解讲义、可操作的实操指南、以及一套自测题。
> 重点看这里——**每一条知识引用都带溯源核验状态**，绿色『已验证』表示它确实来自知识库并被核对过；引用已经自动去重，不会堆砌重复证据。
> 更关键的是这块**裁判团三维度量化评分**：事实准确性、逻辑完整性、教学适用性，分别由三位独立裁判打分。系统不是『感觉答得不错』，而是用可量化的标准审核每一份输出——这是它可信、可控的根基。"

### 镜头 5 · 测验与自适应闭环（5:20–7:00）★ 赛题核心

**画面**:板块③「学习效果测试」弹窗——5 道题(选择题/简答混排)。提交后:

- **顶部状态**:绿色 ✓「测试提交成功! 正确率: X%」+ 黄色横幅「正确率低于 85%,触发自适应降档」(或达 85% 显示「达 85%,触发进阶挑战」)——**唯一判定门槛是 0.85**,公平透明。
- **降档/进阶触发**:整段紫蓝渐变提示卡 + 一个超大按钮「生成更简单的题目」(降档) 或「开始进阶挑战」(进阶)。
- **追问区块**:紫色边框「💡 系统想进一步问你:」列表 1~2 条启发式追问(如「中文和英文在 Token 切分上有什么不同?」「Token 数量对模型的上下文长度和调用成本有什么影响?」),灰色小字「引导式追问,不计分;点击题可作为新提问开启下一轮学习」。
- **进度**:**答对 X / 5 题** + 「重新测试」/「关闭测试」两个大按钮。
- **学习反馈**:底部「学习反馈」三按钮——「👍 内容有帮助」「⚠️ 内容错误」「💬 难度不匹配」(评后端跑 feedback 路由)。

> 截图实拍(2026-08-24,已核对):测验以模态弹窗呈现,无「降维重讲资源包」/「进阶挑战题」独立弹窗——前端改用**「生成更简单题目」按钮**作为降档交互,「系统想进一步问你」追问始终展示。

  **操作**（按实际录制顺序：**追问 → 进阶 → 降维**）：先特写答完题后的「💡 系统想进一步问你」追问区块（约 5 秒）；再放「进阶挑战」回合（剪掉等待）；最后放「降维」回合（剪掉等待）。三个回合各自独立画面，剪辑时按此顺序衔接。
  **旁白**（加长完整版，口播约 1.5~2 分钟；如剪辑时长紧张可整段删掉「学习反馈」小节）：

> "学完之后，系统并不会直接放你走——它会立刻用 5 道题测验你刚刚学到的内容。
> 判分的标准清晰而透明：**以正确率 85% 为进阶门槛**。达到，就向上走；没达到，系统会陪你把基础再夯实一遍。
>
> 测验一结束，它做的第一件事不是打分收场，而是根据你答错的每一道题，找到你的薄弱点，**抛给你一两个启发式追问**。
> 比如，刚才讲到 Token 与词的区别，它会顺着问：『中文和英文在 Token 切分上有什么不同？为什么同一句话，中文常常被切成更多的 Token？』『既然 Token 不等于词，那它对模型的上下文长度和调用成本有什么影响？』
> 这些问题不是考试，不计分——它们是一种引导，让你顺着刚才的思路，**再多想一步**。因为真正的掌握，不是『记住了』，而是『想通了』。
> 你会发现，追问之后，知识点在你脑子里连成了线，而不是散落成点。
>
> 顺着追问继续往前走——如果你答得足够扎实，正确率跨过了 85% 这条线，系统就会为你**解锁进阶挑战**。
> 进阶题不是把同一道题出得更难，而是把难度整体往上抬一个台阶：它会把多个知识点揉在一起，让你在真实场景里综合运用。
> 这一关，是对你能力边界的试探——跨过去了，你就不再是『学会了一个概念』，而是『能把它用起来了』。
>
> 而如果哪一步没有站稳——比如进阶题里暴露了基础的漏洞，或者正确率没有到 85%——系统也绝不会责怪你。
> 它会立刻**降维**：不再用刚才那套难度，而是为你**重新生成一套更简单的题目**，从你最薄弱的地方重新出发，一题一题地把基础夯实，直到你稳稳地接住为止。
> 降维不是退步，是换一条更缓的坡，让你一步一步走上去。
>
> 还不止这些——测验页的底部，还有三个小小的按钮：『内容有帮助』『内容错误』『难度不匹配』。
> 你可以直接告诉系统：哪里讲得好，哪里讲错了，哪里太难或者太简单。这些反馈会被系统记下来，用于调整下一轮生成——**系统会随着你的反馈，越变越懂你**。
>
> 追问、进阶、降维、反馈——每一步，都不是一套写死的剧本，而是系统根据你的真实作答，实时做出的决定。
> 这就是『自适应』：同一个系统，对每一个人，走出属于他自己的、独一无二的学习路径。"

### 镜头 5.5 · ⭐ 差异化适配实证（7:00–8:30）★ 赛题"差异化画像+适配能力"双硬要求

**画面**：回到「创建学习任务」页，用**同一个系统、同一个页面**，先后载入 **TC-020（中级·开发者）** 与 **TC-048（进阶·开发者）** 两组画像，各完成一次「载入画像 → 提交 → 资源生成完成 → 特写资源」的快速演示（中间等待段全部剪掉，只留提交前与结果页两个瞬间）。

- **TC-020 半段（约 45s）**：
  - 点「学情画像」折叠面板 → 点 **TC-020 中级·开发者** 标签 → 字段自动填充（知识水平=中级、学科背景=开发者、学习目标=项目落地、领域关键词=Agent框架）；
  - 学习问题框自动带入 TC-020 定稿问题「请解释 ReAct（Reasoning+Acting）范式的工作流程，Thought-Action-Observation 循环是如何进行的？」；
  - 提交 → 快进到生成完成（剪等待）→ 切到资源页，特写**中级资源的实操导向**（如实操指南步骤、中间难度的测试题）。
- **TC-048 半段（约 45s）**：
  - 回到创建任务页 → 点 **TC-048 进阶·开发者** 标签 → 字段自动填充（知识水平=进阶、学科背景=开发者、学习目标=项目落地、领域关键词=Agent框架）；
  - 学习问题框自动带入 TC-048 定稿问题「请设计一个多 Agent 协作流程，包含规划 Agent、执行 Agent、审查 Agent，说明数据如何流转。」；
  - 提交 → 快进到生成完成（剪等待）→ 切到资源页，特写**进阶资源的工程架构深度**（多 Agent 协作设计、跨段内容、进阶测试题）。
- **收尾（约 15s）**：可用画中画/快切对比 TC-001 入门讲义 vs TC-048 进阶讲义，让"难度梯度"一眼可见。

**操作（在队友电脑上这样执行）**：
1. 两组画像均**严格复用定稿用例**（数据与 `data/io_examples/bm_TC-0XX_io.json` 一致），不临时编造；
2. 建议**提前用 TC-020/048 各真实跑一遍预热**（与 TC-001 预热同法，不进正片），保证正式拍摄时 KB 已热、出结果快；
3. 每组只录「载入画像→点提交→（等待，剪掉）→结果页特写」三个画面即可，单段实拍约 2~4 分钟（含等待），剪辑后各压到 45 秒。

**旁白**：

> "刚才那条轨迹是**入门画像**（TC-001）——讲法通俗、从零开始。
> 现在我把画像换成 **中级·开发者**（TC-020）：问的是 ReAct 范式怎么工作。注意，同一套系统，生成的内容开始强调**实操步骤**和**中间难度**，不再从头讲起。
> 再换成 **进阶·开发者**（TC-048）：让它设计一个多 Agent 协作流程。看，这次生成的是**工程架构级别的深度内容**——规划、执行、审查三个 Agent 的数据流转都讲到了。
> 同一个系统、三种水平、三种讲法——这就是『**差异化适配**』：不是一套模板应付所有人，而是先读懂你，再给你对应的深度。"

### 镜头 6 · 报告页四图（8:30–9:10）

**画面**：板块④「报告」——四个 tab：学情画像 / 盲区热力图 / 学习路径 / **资源难度匹配曲线**。热力图用三色（绿=已掌握 mastered / 黄=部分掌握 partial / 红=盲区 blind）；难度曲线画「学生水平 vs 资源难度」双线 + 整体匹配率。**操作**：依次点四个 tab，在难度曲线停留久一点（这是赛题硬要求、此前缺失的项）。**旁白**：

> "学了一阵子，系统会给你一份完整的学情报告——不是一页总结，而是四个真正能用来做决策的视图。
> 第一，你的**学情画像**：知识水平、每个领域的掌握程度、历次测试成绩，都在这里，并且会随每一次交互持续更新。
> 第二，**知识盲区热力图**——绿色是已掌握，黄色是部分掌握，红色就是系统定位出你还没碰过的区域，一眼看出该补哪里。
> 第三，**推荐学习路径**：从基础概念、Prompt 工程，到向量检索、RAG 架构、Agent 框架，再到项目实战，一共七个阶段，每个阶段都标好了你的当前状态和预计学时。
> 再看这条——**资源难度匹配曲线**。蓝线是系统评估的你当前的掌握水平，红线是这次给你资源的实际难度。**两条线之间如果有落差，并不是系统做错了——恰恰相反，它说明系统看出了你的真实位置和资源难度之间的偏差**：偏简单了，下一轮就往上抬一点；偏难了，就往下降一点。每个点都会如实标出『贴合 / 偏简单 / 偏难』。所以顶部这个『整体难度匹配率』，它不是一张成绩单，而是一把**持续校准的尺子**——它的意义不在于这一次分数高低，而在于让系统知道下一次该怎么调，让每一次生成，都比上一次更贴近你一点。
> 而且请注意——这份报告**累积了我们刚才演示的三组画像、三个领域的全部学习轨迹**，不是只统计最后一次。
> 评委老师，这条曲线背后每一格数据，都来自真实落库的答题记录和画像更新，是真跑出来的，不是画上去的。"

<details><summary>备用 · 完整丰富版（约 85 秒，若时间轴允许延长镜头 6 时使用）</summary>

> "学了一阵子，系统会给你一份完整的学情报告——它不只是一页总结，而是四个可以真正用来做决策的视图。
> 第一，是你的**学情画像**：知识水平、每个领域的掌握程度、历次测试成绩，都在这里，而且会随着你的每一次提问、每一次测验持续更新。
> 第二，是**知识盲区热力图**。绿色代表已经掌握，黄色是部分掌握，红色就是系统定位出你还没碰过的区域——一眼就能看出该补哪里。而每个节点背后，还关联着负责这个领域的智能体，以及它历史上的表现分：系统不只告诉你哪里不会，还知道该派谁去教你。
> 第三，是**推荐学习路径**：从基础概念、Prompt 工程，到模型调用与微调、向量检索、RAG 架构、Agent 框架，再到项目实战，一共七个阶段，每个阶段都标好了你的当前状态和预计学时，先学什么、后学什么，一目了然。
> 第四，就是这条——**资源难度匹配曲线**。
> 蓝线，是系统评估的你当前的掌握水平；红线，是系统这次给你资源的实际难度。两条线越贴合，说明这次给你的东西越合适。每一个点还会标出匹配状态：贴合良好、偏简单，还是偏难。顶部这个『整体难度匹配率』，就是自适应做得好不好的量化证明。
> 而且请注意——这份报告**累积了我们刚才演示的全部三组画像、三个领域的学习轨迹**，不是只统计最后一次。系统一直记得你走过的每一步。
> 评委老师，这条曲线背后每一格数据，都来自真实落库的答题记录和画像更新，是真跑出来的，不是画上去的。"

</details>

> ⏱ **时间提示**：镜头 6 槽位 40 秒，上面默认版约 50 秒（需把镜头 6 延到 8:30–9:20，从镜头 8 闭环总结里挤 10 秒）。若剪辑紧张，可删掉"学习路径"那一句压缩回 40 秒。完整丰富版（85 秒）需要额外调整时间轴后再用。

> 📌 **拍摄铁律**：三组画像（TC-001/020/048）必须跑在**同一个 session_id** 上，报告才会累积三个领域的数据（report.py 按 session_id 聚合）；否则报告四图只会显示最后一次的数据。

### 镜头 7 · 贡献记忆 / 能力全景（9:10–9:35）

**画面**:板块⑤「多智能体贡献记忆闭环」页(深紫主题)。从上到下分四块:

- **顶部 4 张 KPI 卡片**(横排,深紫渐变):
  - 「🔀 转换权重 α = **0.30**」(依据 39 条记录统计,α 越大越偏重人工标签,越小越偏重实际表现)
  -「👥 活跃 Agent **11** 个,覆盖 10 个子能力维度」
  -「✅ 最高频率 **75%**,通过 15-转 1-3」
  -「📈 最近活跃记录 **20** 条,超过 1 个 Agent」

- **中段 · Agent 贡献排行**(按 importance_score 倒序,横向条形+3 项数值):前 6 名样例——
  1. 项目实战Agent — 综合 0.92 · 准确 85.9% · 参与 98
  2. LLM基础Agent — 综合 0.90 · 准确 84.1% · 参与 294
  3. RAG架构Agent — 综合 0.87 · 准确 85.7% · 参与 100
  4. LangChain组件Agent — 综合 0.70 · 准确 74.4% · 参与 69
  5. 向量数据库Agent — 综合 0.69 · 准确 72.0% · 参与 56
  6. 模型微调Agent — 综合 0.67 · 准确 67.1% · 参与 61
  底部「展开全部(共 45 个,还有 39 个)」按钮。

- **下段 · 最近贡献流**(近 20 条,时间倒序):每条 `Agent · 任务名` + 评分(0-100) + 状态徽标(`✓ 通过` 绿 / `⚠ 低置信通过` 黄) + 时间戳(如 `2026-08-24 13:42:49`)。

- **底部 · Agent 淘汰记录**(红色边框警示):如 `Agent agent_009 · 项目架构与落地` + 触发条件「连续3次 importance_score < 0.5」+ `function_tag = 项目架构与落地` + 时间戳。

> 截图实拍(2026-08-24,已核对):α 值以「转换权重 0.30」形式展示在 KPI 卡片,不是单独数值卡片;淘汰记录真实显示 agent_009(项目架构与落地)。

**操作**：滚动排行榜，特写 11 个 Agent 名字 + 各自擅长领域。**旁白**：

> "最后看系统的『自我进化』能力。每个智能体每次任务的成败、贡献，都被记进贡献记忆，表现差的会被淘汰、表现好的权重提升。
> 这里能看到 **11 个领域智能体**的排行——它们覆盖从 RAG、Prompt 工程到 Agent 框架等十个 AI/软件开发子方向。刚才的演示里，我们实际用到了学情诊断、LLM 基础、Agent 框架等多个领域的智能体——而系统的能力远不止于此，是跨领域通用的。"

### 镜头 8 · 闭环总结（9:35–9:55）

**画面**：快速回放前面关键帧（画像→8 步主链路调度→资源三形态→测验自适应→报告四图），可用画中画或快切。**旁白**：

> "回头看这一圈：从你的学情画像出发，经过多智能体协同调度、量化裁判审核、可追溯的资源生成，再到自适应测验和持续追踪的报告——**这就是一个完整的、可闭环的个性化辅导系统**。每一个环节都是真实后端跑出来的，不是动画。"

### 镜头 9 · 创新点收尾（9:55–10:00）

**画面**：定格在系统标题 + 一句 slogan；可叠加工具实测指标（从报告取值）。**旁白**：

> "三个我们觉得值得一提的创新：第一，**多智能体辩论 + 溯源核验**来抑制幻觉，让答案可追溯、可验证；第二，**启发式追问**，把『教完就结束』变成『引导你深入』；第三，**量化裁判团**，用三维打分替代主观判断。
> 在实测中，系统的适配率达到【填适配率】、幻觉率控制在【填幻觉率】、谬误率【填谬误率】；唯一未达满分线的是知识覆盖率【填覆盖率】，我们也如实标注。
> 以上就是我们的作品，谢谢观看。"

---

## 三、录制实操要点（给队友/你本人）

1. **固定 session**：录制全程锁定一个 `session_id`，别用 `Date.now()` 每次新建，否则报告零散。
2. **先预热后录制**：预热轮（降维+进阶）不进正片，只为填数据和热 KB。
3. **剪等待**：正片里问答的 84 秒等待段全部剪掉，只留「提交→出结果」瞬间 + 旁白。
4. **真后端验证**：开录前确认前端状态来自 `GET /api/status` 轮询、接口返回 200，且能看到真实 KB 检索日志（说明连的是真后端）。
5. **横幅与调试信息**：录制时隐藏任何后端原始 JSON / 调试横幅，保持界面干净。
6. **三组数据**：主线用 TC-001（LLM 基础，问题「什么是 Token？」）完整跑；**镜头 5.5 用 TC-020（ReAct）与 TC-048（多 Agent 协作）各跑一遍真实生成**（等待段剪掉，各留约 45 秒），形成"入门/中级/进阶"三级差异化实证。三组问题原文均在 `data/io_examples/bm_TC-0XX_io.json`，录制时严格照念，不得改写。
7. **指标取值**：所有百分比从最新 benchmark 报告念出，写入本计划占位符，录制时照念。

---

## 四、评分对应自检表（交卷前勾）


| 维度     | 分值 | 本视频是否覆盖 | 证据镜头                                               |
| -------- | ---- | -------------- | ------------------------------------------------------ |
| 完整性   | 30   | ✅             | 镜头 2–8 完整闭环                                     |
| 创新性   | 25   | ✅             | 镜头 3（多Agent）/ 4（量化裁判）/ 9（辩论消幻觉+追问） |
| 用户体验 | 15   | ✅             | 全程真实前端操作、界面清晰、无演示数据                 |
| 实用价值 | 30   | ✅             | 镜头 5.5 三组画像差异化实证 + 指标表（覆盖率一项如实呈现） |

> 诚实是唯一不能妥协的红线：覆盖率缺口照实说，绝不改匹配口径造假。
> v2 增补说明：镜头 5.5 加入 TC-020/048 实拍后，"对不同背景学习者的适配能力"（答题要求①）与"差异化学习者学情画像输入"（视频要求）双硬指标均有实证画面。
