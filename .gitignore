__pycache__/
*.pyc
venv/
.venv/
.env
.vscode/
.idea/
*.log
logs/

# 数据文件
data/*.db
data/chroma_db/
data/*.sqlite3

# 知识库预计算向量数据（派生产物，从网盘获取，不入 Git）
# vectors.npy 140MB 超过 GitHub 单文件 100MB 限制
data/numpy_kb/
data/raw_docs/
*.npy

# bge-m3 模型本地缓存（2.2GB，通过 git clone https://hf-mirror.com/BAAI/bge-m3 下载）
# 不入 Git，各人本地下载
data/bge_m3_model/

# 测试
.pytest_cache/
htmlcov/
.coverage

# 系统
.DS_Store
Thumbs.db

# 后端运行时数据库（SQLite，运行时生成，不入 Git）
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal

# 调试/敏感残留（不入 Git）
backend/tests/debug_candidate.py
backend/.env.py

# 运行时数据库备份 / 共享内存（不入 Git）
data/*.db-shm
data/*.db-wal
data/*.db.bak_*

# 二进制穿透工具（cloudflared，本地下载，不入 Git）
cloudflared.exe/

# 个人调试 / 代理残留（不入 Git）
new_sub.yaml
diag_resp*.json
verify_resp.json

# 跨进程任务上下文快照（运行时生成，多 worker 共享，不入 Git）
data/task_contexts/

# 临时调试/一次性演示脚本（不入 Git）
_verify_traceability_tmp.py
e2e_demo.py
e2e_q2_timing.py

